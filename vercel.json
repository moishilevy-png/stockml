{
  "rewrites": [
    { "source": "/auth/callback", "destination": "/api/callback" },
    { "source": "/notifications", "destination": "/api/notifications" }
  ]
}
```

Guardá con **Ctrl+S**, luego en la terminal:
```
git add .
git commit -m "vercel routing"
git push